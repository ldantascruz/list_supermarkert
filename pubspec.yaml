name: list_supermarkert
description: Um novo conceito de lista de supermercado. Abandone o papel e caneta e guarde a sua lista enquuanto já calcula os preços.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.0-262.2.beta <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  mask_text_input_formatter: ^2.5.0
  sqflite: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
